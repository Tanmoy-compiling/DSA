#include <bits/stdc++.h>

using namespace std;

int knapsack(vector<int> weight,vector<int> value,int W,int n){
    if(n==0)
        return 0;
    
        
}

int main(){
    vector<int> weight={};
    vector<int> value={};
    int n = 5;
    int W=10;
    knapsack(weight,value,W,n);
}